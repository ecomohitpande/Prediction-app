fluidPage(
  titlePanel("Product Level Weekly Forecast"),
  sidebarLayout(
    sidebarPanel(
      selectInput("id", "Select The Prduct",choices = c("actual", "g"))
    ), 
    mainPanel(
      tableOutput("out"),
      tableOutput("out1"),
      dygraphOutput("graph"),
      dygraphOutput("graph0")
    )))
